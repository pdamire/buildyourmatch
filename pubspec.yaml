name: build_your_match
description: Build Your Match — Grow first, then meet.
publish_to: "none"
version: 1.0.1+3

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  flutter_riverpod: ^2.5.1
  image_picker: ^1.0.7
  supabase_flutter: ^2.5.6
  purchases_flutter: 9.6.0
  url_launcher: ^6.3.0
  intl: ^0.20.2
  collection: ^1.18.0
  pdf: ^3.10.8
  printing: ^5.12.0
  path_provider: ^2.1.3
  share_plus: ^10.0.2
  flutter_localizations:
    sdk: flutter



dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/challenges_100.csv
  generate: true
